# Pixel Office

**A living pixel-art office for your Grok Bot agent team. Click an agent, hover for a highlight, and watch the floor work.**

Pixel Office renders a Grok Bot team as a busy office floor. Agents wander, sit at desks, type, and share speech bubbles. The roster, statuses, and tasks live in one JSON file.

- Live URL: <https://abdelrahmanmagdii.github.io/pixel-office/>
- Bot template: <https://x.ai/bot/REPLACE_WITH_YOUR_TEMPLATE_LINK>

## 1. What this is

- The office is a shared website. Anyone can open the live URL and click around.
- The Grok Bot is the recipe. The bot template carries instructions, skills, and setup steps. People add the template to Grok Bot with one click.
- The website and the bot work together. The bot writes the roster for the office; the office shows the team.

## 2. Make an office for your team

Use the GitHub template to get your own copy of this project:

1. Open <https://github.com/abdelrahmanmagdii/pixel-office>.
2. Select **Use this template**.
3. Name your repository and create it.
4. GitHub Pages deploys your copy automatically (see `.github/workflows/pages.yml`).
5. Edit `public/agents.json` to match your team.

Each person who follows these steps gets their own floor.

## 3. Customize the roster

The roster lives in `public/agents.json` (`officeTitle`, `subtitle`, `agents[]`). Sprite sheets are `public/assets/characters/agent-{id}.png`. Keep `id` values that match existing art, or add matching PNGs.

To map a bot list into the roster:

```bash
cp bots.example.json bots.json   # edit names, roles, desks
npm run map-bots                 # writes public/agents.json
npm run build
```

## 4. Run locally

```bash
git clone https://github.com/abdelrahmanmagdii/pixel-office.git
cd pixel-office
npm install
npm run dev
```

Build and preview:

```bash
npm run build
npm run preview
```

## 5. Stack

- Vite + TypeScript
- Phaser 4 (pixelArt mode)
- Original PNG sprites under `public/assets/` (see `public/assets/MANIFEST.md`)
- Open Graph image generated by `scripts/make_og_image.py`

## 6. Contest entry

Pixel Office is an entry in the Grok Bot Sharing Contest. See `CONTEST.md` for the entry checklist and `grok-bot/` for the ready-to-paste bot description, skill, and routine.